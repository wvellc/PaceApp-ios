# Copy to functions/.env and fill in from https://www.strava.com/settings/api
# STRAVA_CLIENT_ID is not secret (it ships in the app too); the secret is set
# separately with:  firebase functions:secrets:set STRAVA_CLIENT_SECRET
STRAVA_CLIENT_ID=
